apiVersion: tekton.dev/v1
kind: PipelineRun
metadata:
  annotations:
    build.appstudio.openshift.io/repo: https://github.com/openshift/osde2e?rev={{revision}}
    build.appstudio.redhat.com/commit_sha: '{{revision}}'
    build.appstudio.redhat.com/target_branch: '{{target_branch}}'
    pipelinesascode.tekton.dev/cancel-in-progress: 'false'
    pipelinesascode.tekton.dev/max-keep-runs: '10'
    pipelinesascode.tekton.dev/on-cel-expression: event == "push" && target_branch
      == "main" && ( "test/sdn_migration/***".pathChanged() || ".tekton/sdn-migration-push.yaml".pathChanged()
      || "go.*".pathChanged() )
  creationTimestamp: null
  labels:
    appstudio.openshift.io/application: test-suites
    appstudio.openshift.io/component: sdn-migration
    pipelines.appstudio.openshift.io/type: build
  name: sdn-migration-on-push
  namespace: osde2e-cicada-tenant
spec:
  params:
  - name: git-url
    value: '{{source_url}}'
  - name: revision
    value: '{{revision}}'
  - name: output-image
    value: quay.io/redhat-user-workloads/osde2e-cicada-tenant/test-suites/sdn-migration:{{revision}}
  - name: dockerfile
    value: test/sdn_migration/Dockerfile
  taskRunTemplate:
    serviceAccountName: build-pipeline-sdn-migration
  workspaces:
  - name: git-auth
    secret:
      secretName: '{{ git_auth_secret }}'
  pipelineRef:
    resolver: git
    params:
    - name: url
      value: https://github.com/openshift/boilerplate
    - name: revision
      value: master
    - name: pathInRepo
      value: pipelines/docker-build-oci-ta/pipeline.yaml
status: {}
